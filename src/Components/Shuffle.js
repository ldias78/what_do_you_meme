// import React from "react";

// const shuffleDeck = () => {
//   setDeck(
//     deck
//       .map((a) => [Math.random(), a])
//       .sort((a, b) => a[0] - b[0])
//       .map((a) => a[1])
//   );
// };

// export default shuffleDeck;
